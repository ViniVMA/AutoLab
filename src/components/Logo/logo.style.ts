import styled from 'styled-components';

export const LogoWrapper = styled.a`
    display: flex;
    cursor: pointer;

    span {
        font-size: 38px;
        font-weight: 600;
        color: #F54A48;
        Padding-left: 20px;
    }
`